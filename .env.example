# Configurações do Banco de Dados
DB_NAME=tracking_system
DB_USER=postgres
DB_PASSWORD=sua_senha_super_segura_aqui
DB_PORT=5432

# URL completa do banco (será gerada automaticamente no docker-compose)
DATABASE_URL=postgresql://postgres:sua_senha_super_segura_aqui@localhost:5432/tracking_system

# Configurações da Aplicação
NODE_ENV=production
APP_PORT=3000
PORT=5000

# Segurança
SESSION_SECRET=sua_chave_secreta_muito_longa_e_segura_para_sessoes

# SSL (se usar HTTPS)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/private.key

# Configurações de Deploy
DOMAIN=seudominio.com.br